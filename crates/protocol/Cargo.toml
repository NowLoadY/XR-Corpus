[package]
name = "xr-corpus-protocol"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-only"
repository = "https://github.com/NowLoadY/XR-Corpus"
description = "Stable HTTP protocol shared by XR Corpus servers and clients."

[dependencies]
serde = { version = "1.0.229", features = ["derive"] }
xr-corpus-core = { path = "../core" }
